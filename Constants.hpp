//
// Created by eliran on 10/7/20.
//

#ifndef BALLFILTERTASK_CONSTANTS_HPP
#define BALLFILTERTASK_CONSTANTS_HPP

/*
 * This class is a constants or variables which are used throughout the program
 */
unsigned const int ROWS = 80, COLS = 80;
unsigned const int centroid_r = 50, centroid_g = 50, centoid_b = 50;
unsigned const int inside_r = 0, inside_g = 0, inside_b = 255;
unsigned const int outside_r = 255, outside_g = 0, outside_b = 255;

#endif //BALLFILTERTASK_CONSTANTS_HPP
